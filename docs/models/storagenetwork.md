# StorageNetwork

Storage network the mapped server joins to reach the volume over NVMe-TCP. Null until the network has been provisioned for the volume.


## Fields

| Field                                                                                         | Type                                                                                          | Required                                                                                      | Description                                                                                   |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `vid`                                                                                         | *OptionalNullable[int]*                                                                       | :heavy_minus_sign:                                                                            | VLAN ID of the storage VLAN to tag on the server bond.                                        |
| `host_cidr`                                                                                   | *OptionalNullable[str]*                                                                       | :heavy_minus_sign:                                                                            | Storage IP of the mapped server, in CIDR notation. Null until the mapping status is "mapped". |
| `gateway`                                                                                     | *OptionalNullable[str]*                                                                       | :heavy_minus_sign:                                                                            | Gateway of the storage network, used for the routes below.                                    |
| `routes`                                                                                      | List[*str*]                                                                                   | :heavy_minus_sign:                                                                            | Storage infrastructure prefixes to route via the gateway.                                     |
| `block_gateway`                                                                               | *OptionalNullable[str]*                                                                       | :heavy_minus_sign:                                                                            | NVMe-oF/TCP discovery portal address.                                                         |
| `block_port`                                                                                  | *OptionalNullable[int]*                                                                       | :heavy_minus_sign:                                                                            | NVMe-oF/TCP discovery portal port.                                                            |