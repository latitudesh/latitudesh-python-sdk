# CreateElasticIPMode

How the elastic IP is delivered. Defaults to routed

## Example Usage

```python
from latitudesh_python_sdk.models import CreateElasticIPMode

value = CreateElasticIPMode.ROUTED
```


## Values

| Name     | Value    |
| -------- | -------- |
| `ROUTED` | routed   |
| `BGP`    | bgp      |