# CreateServerServersBilling

The server billing type. Accepts `hourly` and `monthly` for on demand projects and `yearly` for reserved projects.

## Example Usage

```python
from latitudesh_python_sdk.models import CreateServerServersBilling

value = CreateServerServersBilling.HOURLY
```


## Values

| Name      | Value     |
| --------- | --------- |
| `HOURLY`  | hourly    |
| `MONTHLY` | monthly   |
| `YEARLY`  | yearly    |