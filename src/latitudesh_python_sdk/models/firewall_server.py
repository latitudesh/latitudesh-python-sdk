"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from latitudesh_python_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class FirewallServerType(str, Enum):
    FIREWALL_ASSIGNMENTS = "firewall_assignments"


class FirewallServerServerTypedDict(TypedDict):
    r"""Present only when the assignment targets a server."""

    id: NotRequired[str]
    hostname: NotRequired[str]
    primary_ipv4: NotRequired[Nullable[str]]


class FirewallServerServer(BaseModel):
    r"""Present only when the assignment targets a server."""

    id: Optional[str] = None

    hostname: Optional[str] = None

    primary_ipv4: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "hostname", "primary_ipv4"])
        nullable_fields = set(["primary_ipv4"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class FirewallServerVirtualMachineTypedDict(TypedDict):
    r"""Present only when the assignment targets a virtual machine."""

    id: NotRequired[str]
    hostname: NotRequired[str]
    primary_ipv4: NotRequired[Nullable[str]]


class FirewallServerVirtualMachine(BaseModel):
    r"""Present only when the assignment targets a virtual machine."""

    id: Optional[str] = None

    hostname: Optional[str] = None

    primary_ipv4: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "hostname", "primary_ipv4"])
        nullable_fields = set(["primary_ipv4"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class FirewallServerAttributesTypedDict(TypedDict):
    server: NotRequired[FirewallServerServerTypedDict]
    r"""Present only when the assignment targets a server."""
    virtual_machine: NotRequired[FirewallServerVirtualMachineTypedDict]
    r"""Present only when the assignment targets a virtual machine."""
    firewall_id: NotRequired[str]


class FirewallServerAttributes(BaseModel):
    server: Optional[FirewallServerServer] = None
    r"""Present only when the assignment targets a server."""

    virtual_machine: Optional[FirewallServerVirtualMachine] = None
    r"""Present only when the assignment targets a virtual machine."""

    firewall_id: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["server", "virtual_machine", "firewall_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FirewallServerDataTypedDict(TypedDict):
    id: NotRequired[str]
    type: NotRequired[FirewallServerType]
    attributes: NotRequired[FirewallServerAttributesTypedDict]


class FirewallServerData(BaseModel):
    id: Optional[str] = None

    type: Optional[FirewallServerType] = None

    attributes: Optional[FirewallServerAttributes] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "type", "attributes"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FirewallServerTypedDict(TypedDict):
    data: NotRequired[FirewallServerDataTypedDict]


class FirewallServer(BaseModel):
    data: Optional[FirewallServerData] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["data"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
