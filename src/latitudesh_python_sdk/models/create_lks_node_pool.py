"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .lks_node_pool_taint import LksNodePoolTaint, LksNodePoolTaintTypedDict
from enum import Enum
from latitudesh_python_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class CreateLksNodePoolType(str, Enum):
    LKS_NODE_POOLS = "lks_node_pools"


class CreateLksNodePoolDataType(str, Enum):
    BARE_METAL = "bare_metal"


class CreateLksNodePoolAttributesTypedDict(TypedDict):
    plan: str
    r"""Plan the pool nodes are provisioned from"""
    count: int
    r"""Number of nodes; must be >= 1"""
    type: NotRequired[CreateLksNodePoolDataType]
    kubernetes_version: NotRequired[str]
    r"""Patch; defaults to the cluster control-plane patch. Must not be newer than the control-plane patch (422 VERSION_SKEW)"""
    max_pods_per_node: NotRequired[int]
    r"""kubelet --max-pods for every node in the pool. Set once, at creation: it is immutable afterwards and a PATCH that carries it is rejected with 422. Omit to get the platform default (110)."""
    name: NotRequired[str]
    r"""Optional; generated when omitted"""
    description: NotRequired[str]
    labels: NotRequired[Dict[str, str]]
    r"""Kubernetes labels (max 50). Keys use Kubernetes label-key syntax; reserved prefixes (kubernetes.io, k8s.io, cluster.x-k8s.io, lks.latitude.sh and their subdomains) are rejected with 422. Values may be empty. The whole map is stored as sent."""
    taints: NotRequired[List[LksNodePoolTaintTypedDict]]
    r"""Kubernetes taints (max 50). Each (key, effect) pair must be unique. Key follows the same syntax and reserved-prefix rules as a label key."""


class CreateLksNodePoolAttributes(BaseModel):
    plan: str
    r"""Plan the pool nodes are provisioned from"""

    count: int
    r"""Number of nodes; must be >= 1"""

    type: Optional[CreateLksNodePoolDataType] = None

    kubernetes_version: Optional[str] = None
    r"""Patch; defaults to the cluster control-plane patch. Must not be newer than the control-plane patch (422 VERSION_SKEW)"""

    max_pods_per_node: Optional[int] = None
    r"""kubelet --max-pods for every node in the pool. Set once, at creation: it is immutable afterwards and a PATCH that carries it is rejected with 422. Omit to get the platform default (110)."""

    name: Optional[str] = None
    r"""Optional; generated when omitted"""

    description: Optional[str] = None

    labels: Optional[Dict[str, str]] = None
    r"""Kubernetes labels (max 50). Keys use Kubernetes label-key syntax; reserved prefixes (kubernetes.io, k8s.io, cluster.x-k8s.io, lks.latitude.sh and their subdomains) are rejected with 422. Values may be empty. The whole map is stored as sent."""

    taints: Optional[List[LksNodePoolTaint]] = None
    r"""Kubernetes taints (max 50). Each (key, effect) pair must be unique. Key follows the same syntax and reserved-prefix rules as a label key."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "type",
                "kubernetes_version",
                "max_pods_per_node",
                "name",
                "description",
                "labels",
                "taints",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateLksNodePoolDataTypedDict(TypedDict):
    type: CreateLksNodePoolType
    attributes: CreateLksNodePoolAttributesTypedDict


class CreateLksNodePoolData(BaseModel):
    type: CreateLksNodePoolType

    attributes: CreateLksNodePoolAttributes


class CreateLksNodePoolTypedDict(TypedDict):
    data: CreateLksNodePoolDataTypedDict


class CreateLksNodePool(BaseModel):
    data: CreateLksNodePoolData
