"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .lks_node_pool_taint import LksNodePoolTaint, LksNodePoolTaintTypedDict
from enum import Enum
from latitudesh_python_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class UpdateLksNodePoolType(str, Enum):
    LKS_NODE_POOLS = "lks_node_pools"


class UpdateLksNodePoolAttributesTypedDict(TypedDict):
    r"""At least one attribute must be provided. max_pods_per_node is set once, at creation, and is not updatable: a PATCH that carries it is rejected with 422."""

    name: NotRequired[str]
    count: NotRequired[int]
    r"""must be >= 1"""
    kubernetes_version: NotRequired[str]
    r"""A newer patch rolls a node-recreating upgrade. Must not be lower than the current patch (422 DOWNGRADE_NOT_ALLOWED) nor newer than the control-plane patch (422 VERSION_SKEW)"""
    labels: NotRequired[Dict[str, str]]
    r"""Declarative replace. Omit to leave labels unchanged; send the whole map to replace it (not a merge); send {} to clear all. Same validation as on create."""
    taints: NotRequired[List[LksNodePoolTaintTypedDict]]
    r"""Declarative replace. Omit to leave taints unchanged; send the whole list to replace it; send [] to clear all. Same validation as on create."""
    description: NotRequired[str]


class UpdateLksNodePoolAttributes(BaseModel):
    r"""At least one attribute must be provided. max_pods_per_node is set once, at creation, and is not updatable: a PATCH that carries it is rejected with 422."""

    name: Optional[str] = None

    count: Optional[int] = None
    r"""must be >= 1"""

    kubernetes_version: Optional[str] = None
    r"""A newer patch rolls a node-recreating upgrade. Must not be lower than the current patch (422 DOWNGRADE_NOT_ALLOWED) nor newer than the control-plane patch (422 VERSION_SKEW)"""

    labels: Optional[Dict[str, str]] = None
    r"""Declarative replace. Omit to leave labels unchanged; send the whole map to replace it (not a merge); send {} to clear all. Same validation as on create."""

    taints: Optional[List[LksNodePoolTaint]] = None
    r"""Declarative replace. Omit to leave taints unchanged; send the whole list to replace it; send [] to clear all. Same validation as on create."""

    description: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["name", "count", "kubernetes_version", "labels", "taints", "description"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class UpdateLksNodePoolDataTypedDict(TypedDict):
    type: UpdateLksNodePoolType
    attributes: NotRequired[UpdateLksNodePoolAttributesTypedDict]
    r"""At least one attribute must be provided. max_pods_per_node is set once, at creation, and is not updatable: a PATCH that carries it is rejected with 422."""


class UpdateLksNodePoolData(BaseModel):
    type: UpdateLksNodePoolType

    attributes: Optional[UpdateLksNodePoolAttributes] = None
    r"""At least one attribute must be provided. max_pods_per_node is set once, at creation, and is not updatable: a PATCH that carries it is rejected with 422."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class UpdateLksNodePoolTypedDict(TypedDict):
    data: UpdateLksNodePoolDataTypedDict


class UpdateLksNodePool(BaseModel):
    data: UpdateLksNodePoolData
